<h1>ショッピングカート</h1>

<%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete,"data-confirm" => "本当に消しますか？" %>

商品名
単価
数量
小計
<% @cart_item.each do |cart_item| %>

<%= cart_item.item.name %>

<%= cart_item.item.with_tax_price %>

<%= form_with model: @cart_item, url: cart_item_path(cart_item.id), method: :patch do |f| %>

<%= f.select :amount, *[1..10] %>
<%= f.submit"変更" %>

<% end %>

<%= cart_item.subtotal %>

<%= link_to "削除する", cart_item_path(cart_item.id), method: :delete,"data-confirm" => "本当に消しますか？" %>


<% @total +=  cart_item.subtotal %>

<% end %>
合計金額
<%= @total %>
<%= link_to "買い物を続ける", root_path %>
<%= link_to "情報入力に進む", new_order_path %>

